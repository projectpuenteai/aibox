fastapi==0.111.0
# FastAPI 0.111.0 supports both pydantic v1 and v2. Pin v2 explicitly so a
# rebuild months from now can't silently pull a different major version and
# drift schema-validation behaviour.
pydantic>=2.0,<3.0
uvicorn[standard]==0.30.1
docker==7.1.0
httpx==0.27.0
argon2-cffi==23.1.0
cryptography==43.0.1
chromadb==1.5.5
sentence-transformers==5.2.2
transformers==4.46.3
tokenizers==0.20.3
